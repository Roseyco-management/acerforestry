---
phase: 03-core-layout-navigation
plan: 01
type: execute
---

<objective>
Build responsive header component with desktop navigation and mobile menu functionality.

Purpose: Establish site-wide navigation that provides easy access to all pages on desktop and mobile devices, supporting the three-funnel architecture.
Output: Working Header component with responsive navigation, mobile menu, and consistent branding across all breakpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-03-SUMMARY.md

**Tech stack available:** Next.js 15.5.12, React 19, TypeScript, Tailwind CSS, clsx
**Established patterns:** Component composition, variant-based styling, Tailwind className merging, TypeScript prop interfaces
**Components available:** Button, Container, Heading, Section components in src/components/ui/

**Key files:**
@src/components/ui/Button.tsx
@src/components/ui/Container.tsx
@src/app/layout.tsx

**Navigation structure from roadmap:**
- Home (/)
- Forest Managers (/forest-managers) - primary client funnel
- Services (/services) - service showcase
- Subcontractors (/subcontractors) - recruitment funnel
- Training (/training) - educational content

**Contact information from PROJECT.md:**
- Phone: 07756513670
- Email: dillan.hill@acerforestry.co.uk
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with desktop navigation</name>
  <files>src/components/layout/Header.tsx</files>
  <action>Create Header component in src/components/layout/ directory (create layout/ folder first). Use Container component for max-width constraints. Include Acer Forestry logo/text on the left (use primary green color #1B4332). Add desktop navigation links on the right: Home, Forest Managers, Services, Subcontractors, Training. Style links with hover effects using primary/secondary colors. Add phone number (07756513670) as a prominent CTA in the header using the Button component with primary variant. Use Tailwind's hidden/flex utilities to hide mobile menu button on desktop (lg:flex). Export as default function. Use TypeScript for props if needed (likely just className for override).</action>
  <verify>Import Header in src/app/design-system/page.tsx temporarily and check desktop layout looks correct</verify>
  <done>Header component created with logo, 5 nav links, phone CTA button, responsive at desktop breakpoint (lg:)</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile menu with hamburger toggle</name>
  <files>src/components/layout/Header.tsx</files>
  <action>Add mobile menu functionality to existing Header component. Use React useState hook to manage menu open/closed state. Add hamburger button (three horizontal lines icon using Tailwind border classes or SVG) visible only on mobile (lg:hidden). When clicked, toggle mobile menu visibility. Mobile menu should be full-width dropdown below header with vertical list of all navigation links plus phone CTA button. Style mobile menu with primary background, offwhite text. Use Tailwind's hidden class with conditional rendering based on state. Ensure mobile menu closes when a link is clicked (add onClick handler). Use smooth transitions (transition-all duration-300) for menu open/close animation.</action>
  <verify>npm run dev, test on mobile viewport (resize browser to <1024px), verify hamburger shows, menu toggles open/closed, all links visible and clickable</verify>
  <done>Mobile menu works with hamburger toggle, all links accessible on mobile, smooth transitions, menu closes on link click</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without TypeScript errors
- [ ] Header displays correctly on desktop (logo left, nav links right)
- [ ] Mobile menu toggles correctly with hamburger button
- [ ] All 5 navigation links plus phone CTA present and functional
- [ ] No layout shifts or visual glitches on resize
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Header component fully responsive (mobile and desktop)
- No errors or warnings introduced
- Navigation structure matches roadmap pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-layout-navigation/03-01-SUMMARY.md`:

# Phase 03 Plan 01: Responsive Header & Navigation Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description
- `path/to/another.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md (Footer & Layout Integration)
</output>
