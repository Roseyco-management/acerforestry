---
phase: 03-core-layout-navigation
plan: 02
type: execute
---

<objective>
Create footer component and integrate Header + Footer into reusable MainLayout wrapper for consistent site structure.

Purpose: Complete the core navigation shell with footer contact information and establish layout pattern that all pages will use.
Output: Footer component with contact details, MainLayout wrapper combining Header + Footer, root layout updated to use MainLayout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-layout-navigation/03-01-SUMMARY.md

**Tech stack available:** Next.js 15.5.12, React 19, TypeScript, Tailwind CSS, clsx
**Established patterns:** Component composition, variant-based styling, Tailwind className merging
**Components available:** Button, Container, Heading, Section, Header (from 03-01)

**Key files:**
@src/components/layout/Header.tsx
@src/components/ui/Container.tsx
@src/app/layout.tsx

**Contact information from PROJECT.md:**
- Company: Acer Forestry Ltd (SC670730)
- Directors: Rob and Dillan (26 years combined experience)
- Phone: 07756513670
- Email: dillan.hill@acerforestry.co.uk
- Service areas: Highlands, Perthshire, Morayshire, Scotland

**Design system colors:**
- Primary: #1B4332 (dark green)
- Offwhite: #FAF9F6
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer component with contact information</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>Create Footer component in src/components/layout/. Use Container component for max-width constraints. Design footer with dark background (bg-neutral or bg-primary) and offwhite text. Include three sections: Company info (left) - "Acer Forestry Ltd (SC670730)", "Rob and Dillan - 26 years combined experience", service areas. Contact details (center) - Phone (07756513670) as clickable tel: link, Email (dillan.hill@acerforestry.co.uk) as clickable mailto: link. Quick links (right) - Same navigation structure as header (Home, Forest Managers, Services, Subcontractors, Training). Add copyright notice at bottom: "© 2026 Acer Forestry Ltd. All rights reserved." Make footer responsive - stack sections vertically on mobile (flex-col), horizontal on desktop (lg:flex-row). Use Tailwind spacing (py-12, px-4) for comfortable padding. Export as default function.</action>
  <verify>Import Footer in design-system page temporarily, verify all contact info visible, links work (mailto:, tel:), responsive layout stacks correctly on mobile</verify>
  <done>Footer component created with company info, contact details, quick links, copyright notice, fully responsive</done>
</task>

<task type="auto">
  <name>Task 2: Create MainLayout wrapper component</name>
  <files>src/components/layout/MainLayout.tsx</files>
  <action>Create MainLayout component in src/components/layout/. This component wraps pages with Header and Footer. Structure: Header at top (position sticky optional for later), main content area with children prop in the middle (min-h-screen to ensure footer stays at bottom), Footer at bottom. Use TypeScript interface for props: { children: ReactNode, className?: string }. The main content area should have flex-grow to push footer down if content is short. Add semantic HTML: header tag for Header component, main tag for content, footer tag for Footer component. This creates consistent layout shell for all pages. Export as default function.</action>
  <verify>Import MainLayout in design-system page, wrap some test content, verify Header at top, content in middle, Footer at bottom, no layout issues</verify>
  <done>MainLayout component created wrapping Header + children + Footer with proper semantic HTML and flex layout</done>
</task>

<task type="auto">
  <name>Task 3: Update root layout to use MainLayout</name>
  <files>src/app/layout.tsx</files>
  <action>Update src/app/layout.tsx to wrap {children} with MainLayout component. Import MainLayout from @/components/layout/MainLayout. Replace the current body children structure with MainLayout wrapping children. This applies Header + Footer to all pages automatically via Next.js layout system. Keep existing metadata, font imports, and globals.css import. The structure should be: body > MainLayout > {children}. This is the standard Next.js pattern for site-wide layouts.</action>
  <verify>npm run dev, visit / and /design-system pages, verify Header and Footer now appear on all pages automatically</verify>
  <done>Root layout updated with MainLayout, Header + Footer now appear on all pages, no layout breaks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without TypeScript errors
- [ ] Footer displays all contact information correctly
- [ ] MainLayout renders Header, content, Footer in correct order
- [ ] All pages now have Header + Footer via root layout
- [ ] Responsive behavior works on mobile and desktop
- [ ] No console warnings or errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 3 complete (both plans done)
- Core navigation structure established site-wide
- Footer with all contact details present
- All pages automatically use MainLayout via root layout
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-layout-navigation/03-02-SUMMARY.md`:

# Phase 03 Plan 02: Footer & Layout Integration Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description
- `path/to/another.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

**Phase 3 complete (2/2 plans).**

Core layout and navigation established:
- ✓ Responsive Header with desktop and mobile navigation
- ✓ Footer with company info and contact details
- ✓ MainLayout wrapper for consistent page structure
- ✓ All pages automatically include Header + Footer

Ready for **Phase 4: Forest Manager Funnel** - the primary client conversion page.
</output>
