---
phase: 01-foundation-setup
plan: 01
type: execute
---

<objective>
Initialize Next.js 14+ project with TypeScript and Tailwind CSS, establishing the technical foundation for the Acer Forestry website.

Purpose: Create a modern, type-safe, performant React framework optimized for SEO and rural connectivity, providing the foundation all subsequent phases depend on.
Output: Working Next.js 14+ application with TypeScript, Tailwind CSS, ESLint, and verified build/dev server.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech Stack Decision:** Next.js 14+ chosen for SEO (critical for Forest Manager discoverability), Vercel deployment simplicity, and excellent performance on slower rural connections.

**Mobile-First Constraint:** Forest managers access from phones in the field - responsive design and fast loading are non-negotiable.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14+ with TypeScript and Tailwind CSS</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm` (answer yes to initialize in current directory). This creates Next.js 14+ with App Router, TypeScript, Tailwind CSS, and ESLint pre-configured. Verify src/ directory structure created with app/ folder inside. Update globals.css to remove default starter styles, keeping only Tailwind directives.</action>
  <verify>Verify files exist: package.json shows next@14+, typescript, tailwindcss dependencies; tsconfig.json has proper paths; src/app/layout.tsx and page.tsx exist</verify>
  <done>Next.js 14+ initialized with TypeScript and Tailwind CSS, package.json shows correct dependencies, src/app structure exists</done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint and Prettier for code quality</name>
  <files>.eslintrc.json, .prettierrc, .prettierignore</files>
  <action>Extend existing .eslintrc.json with recommended Next.js rules (already includes next/core-web-vitals). Create .prettierrc with config: { "semi": false, "singleQuote": true, "tabWidth": 2, "trailingComma": "es5", "printWidth": 80, "plugins": ["prettier-plugin-tailwindcss"] }. Install prettier and prettier-plugin-tailwindcss as dev dependencies. Create .prettierignore with: .next, node_modules, dist, build, .git. This ensures Tailwind class sorting and consistent formatting.</action>
  <verify>Run `pnpm add -D prettier prettier-plugin-tailwindcss` succeeds, .prettierrc and .prettierignore files exist, `pnpm exec prettier --check .` runs without errors</verify>
  <done>.prettierrc configured with Tailwind plugin, .prettierignore excludes build artifacts, prettier installed in package.json</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and development server</name>
  <files>None (verification only)</files>
  <action>Run `pnpm run dev` and verify server starts on localhost:3000 without errors. Stop dev server. Run `pnpm run build` to verify production build succeeds without TypeScript or build errors. Run `pnpm run lint` to confirm ESLint passes. This validates the foundation is working correctly.</action>
  <verify>Dev server starts successfully, build completes without errors, lint passes with no warnings, TypeScript compilation succeeds</verify>
  <done>Development server runs, production build succeeds, ESLint passes, no TypeScript errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm run dev` starts without errors
- [ ] `pnpm run build` completes successfully
- [ ] `pnpm run lint` passes with no errors
- [ ] TypeScript compilation has no errors
- [ ] localhost:3000 shows Next.js starter page
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Next.js 14+ initialized with App Router
- TypeScript, Tailwind CSS, ESLint, Prettier configured
- Build and dev server verified working
- No errors or warnings in output
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`:

# Phase 01 Plan 01: Next.js Foundation Summary

**Next.js 14+ foundation established with TypeScript, Tailwind CSS, and quality tooling configured**

## Accomplishments

- Initialized Next.js 14+ with App Router, TypeScript, and Tailwind CSS
- Configured ESLint and Prettier with Tailwind class sorting
- Verified build pipeline and development server working

## Files Created/Modified

- `package.json` - Next.js 14+, TypeScript, Tailwind dependencies
- `tsconfig.json` - TypeScript configuration with path aliases
- `tailwind.config.ts` - Tailwind CSS configuration
- `src/app/layout.tsx` - Root layout component
- `src/app/page.tsx` - Home page component
- `src/app/globals.css` - Global styles with Tailwind directives
- `.eslintrc.json` - ESLint rules extended
- `.prettierrc` - Prettier config with Tailwind plugin
- `.prettierignore` - Prettier ignore patterns

## Decisions Made

[Document any decisions made during execution, or "None"]

## Issues Encountered

[Document any problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (Dependencies & Structure)
</output>
